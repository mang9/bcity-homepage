# B-CITY 홈페이지 — 개발 인계 문서

춘천기업혁신파크 B-CITY 마케팅 홈페이지. **이 문서 하나로 시작할 수 있게** 썼습니다.

| | |
|---|---|
| 저장소 | `https://github.com/mang9/bcity-homepage` (public) |
| 배포 | GitHub Pages — **`main` 브랜치 루트를 그대로 서비스**합니다 |
| 라이브 | `https://mang9.github.io/bcity-homepage/` |
| 빌드 의존성 | `@tailwindcss/cli` **하나뿐**입니다. 번들러·프레임워크 없음 |
| Node | 20 이상 (개발 확인은 24.16.0) |

```bash
git clone https://github.com/mang9/bcity-homepage.git
cd bcity-homepage && npm install
npm run build          # CSS + 서브페이지 전체 빌드
```

정적 파일이라 `index.html` 을 그냥 열어도 대부분 동작하지만, 상대 경로 자산 때문에
**로컬 서버로 여는 것을 권합니다** — `python3 tools/devserver.py` (포트 8893).

---

## 1. 무엇이 진짜이고 무엇이 시안인가

인계에서 가장 먼저 알아야 할 구분입니다.

| 대상 | 상태 |
|---|---|
| 메인 + 서브페이지 18종 | **실제 운영 중**입니다. 카피·수치는 기획서 대조를 마친 확정본 |
| 홍보센터 게시물 | **전부 샘플**입니다. 실제 콘텐츠 아님 (아래 §4) |
| 관리자 화면 21종 | **기능 없는 정적 시안**입니다. 화면·필드 명세 전달용 (아래 §5) |
| 문의 폼 전송 | **백엔드 없음.** 지금은 메일 클라이언트를 여는 방식 (아래 §3) |

---

## 2. 소스 구조 — 루트의 `*.html` 을 직접 고치지 마세요

루트의 서브페이지 HTML 은 **전부 빌드 산출물**입니다. 파일 선두에 그 경고 배너가 있고,
다음 빌드에 덮어써집니다.

```
index.html            메인 — 유일하게 손으로 고치는 HTML
src/
  css/app.css         메인용 Tailwind 소스 → assets/css/app.css 로 컴파일
  shared/             ★ 메인과 서브가 **함께 쓰는** 소스 (§2.1)
  sub/
    layout.html       서브페이지 문서 골격
    nav.json          IA 단일 소스 — GNB · LNB · 모바일메뉴 · 브레드크럼 · 푸터가 전부 여기서 나옴
    pages/*.html      선두 <!--build {JSON}--> front-matter + <main> 본문만
    partials/         gnb · hero · footer · svg
    css/  js/         서브 전용
    data/*.json       홍보센터 콘텐츠 (§4)
tools/build/
  pages.mjs           서브페이지 빌더 (의존성 0)
  lint-classes.mjs    클래스 정합성 검사
  admin.mjs           관리자 화면 생성기
```

**서브페이지를 고치려면** `src/sub/pages/<slug>.html` 을 고치고 `npm run build:pages`.
**메뉴를 고치려면** `src/sub/nav.json` 과 `index.html` 의 `siteMap` 배열 **두 곳**을 함께 고칩니다
(메인 메가메뉴만 별도 배열을 씁니다 — 한쪽만 고치면 어긋납니다).

### 2.1 `src/shared/` — 두 화면이 같은 파일을 씁니다

문의 모달과 개인정보처리방침은 메인과 서브페이지가 **한 벌**을 공유합니다.

| 파일 | 메인이 받는 법 | 서브가 받는 법 |
|---|---|---|
| `contact.css` · `gnb-en.css` | `app.css` 의 `@import ... layer(utilities)` | `pages.mjs` 가 인라인 |
| `contact.js` | `<script src="assets/js/contact.js">` (빌드가 복사) | `pages.mjs` 가 인라인 |
| `contact-modal.html` · `privacy-modal.html` | `index.html` 의 `@shared:*` 마커 구간을 빌드가 채움 | `layout.html` 이 파티셜로 삽입 |

> ⚠ **`index.html` 의 `@shared:` 마커 구간과 `assets/js/contact.js` 는 생성물입니다.**
> 직접 고치면 다음 빌드에 덮어써집니다 — `src/shared/` 를 고치세요.

> ⚠ 이 파일들은 **두 개의 다른 리셋 위에서** 돕니다. 메인은 Tailwind preflight,
> 서브는 평문 CSS + 브라우저 기본값입니다. 그래서 `src/shared/contact.css` 는
> 자기가 쓰는 요소의 기본값(`p` 마진 · `line-height` · 폼 요소)을 **스스로 못 박습니다.**
> 그 리셋을 지우면 두 화면이 조용히 갈라집니다 — 실제로 세 번 겪었습니다.

---

## 3. 문의 폼 — 백엔드를 붙이는 지점

`src/shared/contact.js` 의 **한 줄**입니다.

```js
const CT_ENDPOINT = '';   // 예: 'https://api.b-city.kr/inquiry'
```

- **비어 있으면** `mailto:` 로 메일 클라이언트를 엽니다(현재 동작).
- **URL 을 넣으면** 그 주소로 `fetch` POST 합니다. 전송 성공·실패 UI 와 버튼 비활성화까지
  이미 들어 있으니 서버만 받으면 됩니다.

보내는 본문(JSON):

```json
{ "관심분야": "기업입주문의, 투자문의", "회사명/이름": "…", "휴대전화": "010-…",
  "이메일": "…", "문의내용": "…", "마케팅활용동의": "동의" }
```

### 접수 알림 — 담당자 여러 명에게 자동 발송 (2026-09-01 요청)

`등록하기` 를 누르면 **서버가 담당 실무자들에게 메일을 보내야 합니다.** 화면에는
수신 주소를 노출하지 않습니다 — 그래서 문의 폼의 `담당자 연락처` 카드를 걷어냈고,
안내 문구에서도 주소를 뺐습니다.

- 수신자는 **마케팅부 3명 · 사업부 3명, 모두 6명**입니다(2026-09-01 기준).
  ⚠ **명단(실명·주소)은 이 저장소에 적지 않습니다.** 저장소가 public 이고 이 파일도
    `https://mang9.github.io/bcity-homepage/HANDOFF.md` 로 그대로 열립니다 —
    적는 순간 '개인 메일 노출을 숨긴다'는 요청 자체가 무의미해집니다.
    명단은 담당자에게 직접 받아 **서버 환경변수/설정**에 넣으세요.
- ⚠ 지금도 `assets/js/contact.js` 의 `mailto:` 대상에 담당자 주소 **한 개가 공개**돼 있습니다.
  백엔드가 붙기 전까지 문의를 받는 유일한 경로라 남겨 두었습니다. 엔드포인트를 붙이면서
  이 폴백을 지우거나 역할 주소(예: `contact@`)로 바꾸면 그 노출도 사라집니다.
- 지금은 `CT_ENDPOINT` 가 비어 있어 `mailto:` 로 넘깁니다. 엔드포인트가 붙으면
  그 분기는 타지 않으므로, 서버에서 수신자 목록을 관리하면 됩니다.

#### 담당자가 받는 메일의 형식

**지금 `mailto:` 폴백이 만드는 메일과 같은 모양으로 맞춰 주세요.** 백엔드 연결 전후에
받는 메일이 달라지면 담당자가 두 가지 서식을 익혀야 합니다.

```
제목   [B-CITY 문의] {회사명/이름} - {관심분야}
본문   관심분야 : 기업입주문의, 투자문의
       회사명/이름 : 홍길동
       휴대전화 : 010-0000-0000
       이메일 : hong@example.com
       문의내용 : (여러 줄)
       마케팅활용동의 : 동의
       ─────────────
       접수일시 : 2026-09-02 14:30 (KST)
       유입경로 : https://…/invest.html
```

- **`Reply-To` 를 문의자 이메일로 넣어 주세요.** 담당자가 받은 메일에서 바로 회신하면
  문의자에게 가야 합니다. 이게 없으면 주소를 손으로 옮겨 적게 됩니다.
- **`From` 은 도메인 인증(SPF/DKIM)된 발신 계정**을 씁니다. 문의자 주소를 `From` 에
  넣으면 스푸핑으로 판정돼 스팸함으로 갑니다 — 그래서 `Reply-To` 로 푸는 것입니다.
- **수신자 6명은 `BCC`** 로 넣습니다. `To`/`Cc` 면 서로의 주소가 노출되고,
  문의자에게 자동 회신을 함께 보낼 때 그 주소들이 외부로 나갑니다.
- **`접수일시` · `유입경로` 는 서버가 채웁니다.** 폼이 보내는 JSON 에는 없습니다 —
  시각은 서버 시각, 유입경로는 요청의 `Referer`(없으면 `Origin`) 헤더를 쓰면 됩니다.
  어느 페이지에서 연 문의인지가 응대 우선순위를 가릅니다(투자 · 입주 · 홍보가 다릅니다).
- **`마케팅활용동의` 값은 그대로 보존**합니다. 동의 여부가 이후 마케팅 발송의 법적 근거라
  요약하거나 빼면 안 됩니다. `동의` / `미동의` 두 값만 옵니다.
- 문의자에게 보내는 **접수 확인 메일은 아직 요구사항이 아닙니다.** 화면은 이미
  "담당자가 확인 후 연락드립니다" 라고만 안내합니다 — 보낼지는 결정이 필요합니다.
- 발송이 실패해도 **접수 자체는 성공 처리**하고(사용자에게 재입력을 시키지 않습니다)
  재시도 + 로그로 처리해 주세요. 화면은 HTTP 200 이면 성공으로 봅니다.

> ⚠ **개인정보처리방침을 함께 고쳐야 합니다.** 전송 API 사업자가 생기면
> `src/shared/privacy-modal.html` 제5조(처리 위탁) 표에 수탁자로 추가해야 합니다.
> 지금은 메일 서비스(더존비즈온) 한 곳만 적혀 있습니다.

---

## 4. 홍보센터 콘텐츠 — 지금은 샘플입니다

목록·상세는 화면에 하드코딩돼 있지 않고 `src/sub/data/*.json` 에서 읽습니다.
**필드 명세는 `src/sub/data/_SCHEMA.md`** 를 보세요.

```
notice.json  press.json  video.json  gallery.json  publication.json
```

접근 지점은 `tools/build/pages.mjs` 의 **`loadContent(kind)` 함수 하나**입니다.
API 로 바꿀 때 그 함수 내부만 갈아끼우면 되고, 호출부와 템플릿은 그대로입니다
(`async` 로 바꾸고 `build()` 를 `await` 하면 됩니다).

### 샘플 스위치

현재 JSON 의 항목에는 `_sample: true` 가 붙어 있습니다.

```bash
npm run build:pages                        # 샘플 제외 — 배포용(기본)
SHOW_SAMPLES=1 npm run build:pages         # 샘플 포함 — 화면 확인용
touch .claude/SAMPLES                      # 이 사본에서는 항상 샘플 포함
```

> ⚠ **배포본에 샘플이 들어가면 안 됩니다.** 실제 운영 사이트입니다.
> 커밋 절차는 §7 을 반드시 지켜 주세요.

### ⚠⚠ 저장소를 클론하면 목록이 **비어 있고 상세 페이지 파일이 없습니다**

**이걸 모르면 "상세 페이지는 없는 기능" 으로 읽힙니다.** 실제로 그렇게 진행된 적이
있습니다(2026-09-03). 정상 동작이며, 이유는 이렇습니다.

- 커밋되는 것은 **배포 빌드**입니다. 샘플이 전부 `_sample: true` 라 걸러지므로
  목록 5쪽은 `등록된 게시물이 없습니다` 상태로, **상세 페이지는 0개**로 커밋됩니다.
- 상세 페이지는 **빌드 산출물**입니다. 데이터 1건마다 1쪽이 생기므로 저장소에 둘 것이
  아닙니다. 지금 샘플로 빌드하면 **16쪽**이 생깁니다(공지 4 · 언론 12).

**화면을 보려면** 아래 하나를 하세요.

```bash
SHOW_SAMPLES=1 npm run build:pages     # 목록이 채워지고 상세 16쪽이 생깁니다
```

렌더된 결과를 그냥 보고 싶으면 컨펌용 사이트에 이미 올라가 있습니다 —
<https://mang9.github.io/bcity-homepage-review/notice.html> (목록 → 제목 클릭 → 상세).

### 클릭해야 보이는 화면도 HTML 로 열 수 있습니다

확대보기 · 재생 · 공문 보기 · 문의 모달 · 발행물 호버 오버레이는 **JS 가 DOM 을 만들기 때문에**
파일을 열어도 닫힌 상태만 보입니다. 그 상태를 파일 하나로 모아 두었습니다.

<https://mang9.github.io/bcity-homepage-review/dev-states/index.html>

| | |
|---|---|
| 상태 8개 | 공지 목록 · 언론 목록 · 홍보영상 재생 · 갤러리 확대보기 · 발행물 호버 · 사업주체 공문 보기 · 문의 모달 · 약관 모달 |
| 상세 16쪽 | 공지 4 · 언론 12 — 데이터 1건마다 1쪽 |

⚠ **마크업을 베낀 것이 아닙니다.** 사이트의 실제 `lightbox.js` · `contact.js` 가 열어 준
다음 상태라, 그 스크립트가 바뀌면 이 페이지도 따라옵니다.

직접 만들려면 이 저장소에서:

```bash
SHOW_SAMPLES=1 npm run build:pages
node tools/build/dev-states.mjs .          # dev-states/ 생성
```

### 상세 페이지가 있는 종류는 **둘뿐입니다**

| 종류 | 목록 | 제목을 누르면 | 상세 템플릿 |
|---|---|---|---|
| 공지사항 | 행 목록 | **자체 상세 페이지** | `src/sub/pages/_detail-notice.html` |
| 언론보도 | 행 목록 | **자체 상세 페이지** (안에 원문 기사 링크) | `src/sub/pages/_detail-press.html` |
| 홍보영상 | 카드 | 화면 안 **재생**(라이트박스) | 없음 |
| 갤러리 | 카드 | 화면 안 **확대보기**(라이트박스) | 없음 |
| 발행물 | 카드 | 표지 위 **보기 · 내려받기** | 없음 |

- 템플릿 파일 이름이 `_` 로 시작합니다. 빌더가 **일반 페이지 목록에서 제외**하고
  상세 생성에만 씁니다(`tools/build/pages.mjs` 의 `_detail-` 필터). 이름을 바꾸지 마세요.
- 상세 주소는 **`<종류>-<id>.html`** 입니다(`detailPath()`). `id` 가 이미 종류 접두어로
  시작하면 겹쳐 쓰지 않습니다 — `notice-notice-…` 방지.
  예: `notice.json` 의 `id: "site-open"` → `notice-site-open.html`
- 상세는 이전/다음 글 링크와 목록으로 돌아가기를 함께 그립니다(`post-nav`).
- **언론보도 12건은 2쪽으로 나뉩니다**(실측 — 12행이 DOM 에 있고 10행만 보이며 쪽 버튼 `1 2`).
  공지사항 4건은 한 쪽이라 쪽 버튼이 없습니다.
  목록 컨트롤(분류·검색)은 `tools/build/pages.mjs` 의 `SHOW_LIST_FILTERS = false` 로 꺼 둔
  상태이고, **페이지네이션은 그 스위치와 무관하게 동작합니다.**

---

## 5. 관리자 화면 — `admin-design` 브랜치

**기능이 구현되지 않은 정적 HTML 21개**입니다. 화면 구성과 입력 필드를 전달하는 것이 목적입니다.

```bash
git checkout admin-design
npm run build:admin        # admin/ 20개 생성
```

화면 목록과 기획서 대응은 **`admin/README.md`** 에 있습니다. 화면 안의
`<!-- DEV: … -->` 주석이 개발 시 확인할 사항입니다 (`grep -rn 'DEV:' admin/*.html`).

> ⚠ **`admin/` 을 `main` 에 커밋하지 마세요.** GitHub Pages 가 `main` 루트를 그대로
> 서비스하므로, 실제로 막는 것이 없는 로그인 화면이 공개 도메인에 노출됩니다.
> `main` 의 `.gitignore` 가 `/admin/` 을 무시하는 이유입니다.
>
> ⚠ 브랜치를 오가면 `admin/` 이 작업트리에서 사라집니다(한쪽은 추적 파일, 한쪽은 무시 대상).
> 놀라지 마시고 `npm run build:admin` 을 다시 돌리면 됩니다.

---

## 6. 브랜치

| 브랜치 | 내용 | 배포 |
|---|---|---|
| `main` | 운영 배포본. **샘플 없음 · admin 없음** | **O** (Pages) |
| `admin-design` | 관리자 화면 시안 | X |
| `review/20260828-confirm` | 컨펌용 스냅숏 — 샘플 + admin 을 한 번에 볼 수 있게 | X |
| `backup/*` | 대규모 변경 전 롤백 지점 | X |

---

## 7. 커밋 절차 — 이 순서를 지켜 주세요

작업트리에는 샘플이 보이는 편이 편하고, 배포본에는 없어야 합니다. 그래서 스크립트가
**인덱스에는 배포본을, 작업트리에는 샘플을** 남깁니다.

```bash
npm run commit:prep     # 배포 빌드 → git add -A → 샘플 빌드
git commit -m "…"       # ← 인덱스(배포본)가 커밋된다
git push
```

> ⚠⚠ **`commit:prep` 다음에 `git add` 를 다시 하지 마세요.** 작업트리(샘플)가
> 인덱스를 덮어써 샘플이 배포됩니다. 실제로 18시간 동안 라이브에 노출된 적이 있습니다.
>
> 커밋 전 확인 — **`0` 이 아니면 샘플이 섞인 것입니다.**
> ```bash
> git ls-files --cached | grep -cE "^(notice|press)-[0-9a-z-]+\.html"
> ```

`npm run build:deploy` 는 단독으로 돌리지 마세요 — 화면에서 샘플이 사라집니다.

---

## 8. 검사 도구

```bash
npm run build:check    # 생성물이 소스와 일치하는지 + 클래스 정합성 (커밋 전)
npm run lint:classes   # 클래스 정합성만
```

`lint-classes` 는 **페이지마다** "그 페이지가 실제로 싣는 CSS 번들"과 마크업을 대조합니다.
전역 검사가 아니라서, 정의는 어딘가 있는데 그 페이지 번들에는 없는 경우까지 잡습니다.

---

## 9. 인계 시점의 미결 사항

| 항목 | 내용 |
|---|---|
| 홍보센터 콘텐츠 | 전부 샘플. 실제 게시물로 교체 필요 |
| 문의 폼 백엔드 | 미연결 (§3) |
| 개인정보처리방침 | 수탁자 **서버 소재지** 확인 필요 — 국외면 법 제28조의8(국외 이전) 항목 추가 |
| 공문 PDF | 사업주체 페이지의 [보기] 5건 중 일부가 이미지로만 있음 |
| `DIGITAL TWIN` 버튼 | 메인 히어로. 대상 페이지 미정으로 `href="#"` |
| `assets/css/app.css` | 소스보다 낡은 상태가 있었음 — `npm run build:css` 로 갱신 |
| **영문 사이트 범위** | 아래 별항 |
| **파트너사 데이터층** | 아래 별항 |

### 영문 사이트 — 결정은 그때 (2026-09-03)

관리자 파트너사 폼에 **`영문 상호`** 칸이 있습니다. 지금은 **어디에도 쓰이지 않습니다**
(영문 페이지 없음 · `en/` 없음 · GNB 의 EN 은 "준비중입니다" 툴팁). 개발이 기존
기능정의대로 진행 중이라 **칸은 그대로 둡니다.** 영문 사이트를 시작할 때 아래를 함께 정하세요.

- **`영문 상호` 하나로는 부족합니다.** 영문이 필요한 것은 상호뿐 아니라 카드 설명
  (`춘천기업혁신파크 선도사업 선정 기관`), 그룹 설명, 공지·언론 제목과 본문, 페이지 카피
  전부입니다. 지금 관리자 21화면에서 영문 필드는 **이 하나뿐**이고 콘텐츠 데이터
  (`src/sub/data/*.json`)에는 **언어 축이 없습니다.**
- 그래서 정할 것은 "상호를 어디에 두나" 가 아니라 **콘텐츠 필드 전체에 언어를 어떻게 붙이나** 입니다.
  갈래는 둘입니다.
  - **같은 데이터를 읽고 언어만 바꾼다** — 필드마다 `ko`/`en` 쌍을 두거나 파일을 언어별로
    나눕니다. 이때 상호는 **콘텐츠**라 데이터 쪽에 있어야 합니다(메뉴명·그룹 영문
    `ANCHOR PARTNER` 같은 것은 화면이 갖습니다 — 회사 영문 상호와 혼동하지 마세요).
  - **영문 페이지를 따로 만든다** — 그 페이지의 글은 코드에서 편집하므로 `영문 상호` 칸은
    끝까지 쓰이지 않습니다. 그때는 칸을 빼는 편이 낫습니다(운영자가 채워도 안 나옵니다).

### 파트너사 — 아직 데이터층이 없습니다 (2026-09-03)

관리자 파트너사 화면은 **앞으로 만들 데이터층의 시안**입니다. 지금 실물은 이렇습니다.

| 자리 | 어디에 있나 |
|---|---|
| 사업주체 페이지 카드 8장 | `src/sub/pages/company.html` **마크업에 직접** |
| 사업구조도 출자자 5분류 | 같은 파일의 `.bs-holder` 마크업 |
| 메인 푸터 로고 | `index.html` 의 `partners` 배열 |

- `src/sub/data/` 에는 홍보센터 5종만 있고 **partner 파일이 없습니다.** 관리자 화면이
  실제로 동작하려면 §4 의 `loadContent()` 경로로 파트너를 옮기는 작업이 선행돼야 합니다.
- **분류 → 표시 이름 매핑도 마크업에 박혀 있습니다.** 같은 분류가 두 자리에서 다른 이름으로
  나옵니다 — 카드 그룹은 `관계 기관 · 앵커 기업 · 자산관리 · 금융 파트너 · 전략적 파트너`,
  구조도는 `앵커기업·AMC / GI·공공 / FI·금융 / CI·시공 / SI·전략` 입니다.
  관리자에서 고르는 값은 **키**이고 표시 이름은 화면이 각자 가져야 합니다.
- **설명이 두 자리라 필드도 둘입니다**(2026-09-03 분리). `설명` 은 카드 본문 한두 줄,
  `보조 설명` 은 사업구조도에서만 쓰는 10.5px 한 줄입니다. 합치지 마세요.

---

## 10. SEO — 검색엔진 등록과 도메인 이전 (2026-09-08)

검색엔진 등록(구글 서치콘솔 · 네이버 서치어드바이저 · 다음 검색등록)은 **개발자 쪽에서
진행**하기로 정리됐습니다. 이 저장소가 해 둔 것과, 옮길 때 반드시 함께 고쳐야 하는 것을
적어 둡니다.

### 10.1 ⚠⚠ 먼저 알아야 할 것 두 가지

**① 이 저장소의 `robots.txt` 는 지금 아무 효력이 없습니다.**
크롤러는 robots.txt 를 **원점(scheme+host+port) 루트에서만** 읽습니다(RFC 9309 §2.3).

| 주소 | 상태 |
|---|---|
| `https://mang9.github.io/robots.txt` | **404** ← 크롤러가 실제로 읽는 곳 |
| `https://mang9.github.io/bcity-homepage/robots.txt` | 200 ← 이 저장소가 둔 곳, **읽히지 않음** |

그래서 `Disallow` 도 `Sitemap:` 도 동작하지 않습니다. **여러분 도메인은 사이트가 호스트
루트에 있으므로 그대로 정상 동작합니다** — 이 파일의 내용을 그쪽 루트에 두시면 됩니다.

**② 이 사이트(github.io)는 `noindex` 로 내려 뒀습니다.**
같은 콘텐츠가 두 주소에 색인되면 어느 쪽이 대표가 될지 통제할 수 없기 때문입니다.

> ### ⚠⚠ 옮기실 때 **반드시 `noindex` 를 지우세요**
> 안 지우면 **새 사이트 전체가 검색에서 사라집니다.** 두 곳입니다:
> 1. `tools/build/pages.mjs` 의 `const NOINDEX = true;` → **`false`**
> 2. `index.html` 의 `<meta name="robots" content="noindex" />` → **삭제**
>
> 빌드에 정합 검사를 넣어 뒀습니다 — `SITE_URL` 을 실제 도메인으로 바꾸고 `NOINDEX` 를
> 그대로 두면 **빌드가 실패하면서** 무엇을 고쳐야 하는지 알려 줍니다. 그 검사를 지우지 마세요.

### 10.2 도메인을 옮길 때 고칠 곳 — **네 군데**

| # | 파일 | 값 |
|---|---|---|
| 1 | `tools/build/pages.mjs` | `const SITE_URL = '…'` — 서브페이지 `canonical`·OG·`sitemap.xml` 이 **모두 이 한 값**을 씁니다. 뒤에 슬래시 없이 |
| 2 | `index.html` | `canonical` · `og:url` · `og:image` · JSON-LD 의 `url`·`logo` — **메인만 손글씨**라 여기 직접 적혀 있습니다 |
| 3 | `robots.txt` | 마지막 `Sitemap:` 줄 (1번의 `SITE_URL` 과 같은 값이어야 합니다) |
| 4 | 위 10.1 의 `noindex` 두 곳 | |

고친 뒤 `npm run build` 를 돌리면 서브페이지 16개에 일괄 반영됩니다(메인은 손글씨라 2번).
⚠ 한 곳만 바뀌면 `canonical` 이 존재하지 않는 주소를 가리키게 되고, 그러면 검색엔진이
그 페이지를 색인에서 내립니다.

### 10.3 이미 들어가 있는 것 — 새로 만들지 마세요

| 항목 | 위치 |
|---|---|
| `canonical` · Open Graph · `twitter:card` | 서브페이지는 `pages.mjs` 의 `seoBlock()`, 메인은 `index.html` `<head>` |
| 페이지별 `description` (메인 + 서브 16쪽 전부) | 서브는 각 `src/sub/pages/*.html` 선두 front-matter 의 `description`, 메인은 `index.html` |
| 구조화 데이터 JSON-LD (`Organization`) | `index.html` — `alternateName` 에 `비시티` 표기를 넣어 국문 검색에 대응 |
| `sitemap.xml` 자동 생성 | `pages.mjs` — 배포 빌드에서만 만듭니다(샘플 빌드에서는 생성하지 않음) |
| 파비콘 8종 · `site.webmanifest` · `favicon.ico` | `assets/favicon/` + 루트의 `favicon.ico`·`site.webmanifest`. ⚠ `favicon.ico` 는 브라우저가 `<link>` 와 무관하게 **호스트 루트에서 자동 요청**하므로 반드시 루트에 두세요 |

⚠ **`meta keywords` 는 의도적으로 넣지 않았습니다.** 구글이 공식적으로 무시하며
(2009년 웹마스터 블로그 발표) 네이버도 순위 요소로 쓰지 않습니다. 요청이 와도 넣지 마세요 —
효과가 없고 스팸 신호로 읽힐 수 있습니다. `meta description` 도 **순위 요소가 아니라**
검색결과에 보이는 문구를 정하는 값입니다(그래서 넣었습니다).

### 10.4 소유확인 태그 넣을 자리

`index.html` `<head>` 에 주석 처리된 두 줄이 있습니다. 값만 채우고 주석을 벗기세요.

```html
<meta name="google-site-verification" content="…" />
<meta name="naver-site-verification" content="…" />
```

- ⚠ **빈 값으로 살려 두지 마세요** — 소유확인이 실패로 확정되면 콘솔이 재시도를 며칠 막습니다.
- ⚠ **확인된 뒤에도 태그를 지우지 마세요** — 구글·네이버 모두 태그가 사라지면 소유확인이
  해제되고 데이터 수집이 멈춥니다.
- 서브페이지에는 넣지 않아도 됩니다. 소유확인은 등록한 주소의 **첫 화면**만 봅니다.
- HTML 파일 방식(`google<해시>.html`)을 쓰시면 저장소 루트에 그대로 두면 됩니다.
  빌더의 고아 파일 검사는 **빌드 배너가 있는 파일만** 지우므로 손으로 넣은 파일은 안전합니다.

### 10.5 사이트맵 제출

`https://<도메인>/sitemap.xml` — 세 콘솔에 각각 손으로 제출하세요.
(robots.txt 의 `Sitemap:` 줄만으로도 발견되지만, 제출하면 색인 상태를 볼 수 있습니다.)

현재 **17 URL**(메인 + 목록 16)입니다. 공지사항·언론보도의 **상세 페이지는 지금 샘플이라
빠져 있고**, 실제 콘텐츠가 들어오면 자동으로 늘어납니다(§4 참고).

### 10.6 노릴 검색어 — 우선순위와 담당 페이지

현재 `춘천기업혁신파크` 검색 1쪽은 **언론 기사 4건 + 춘천시 공식 페이지**이고
**사업 홈페이지는 하나도 없습니다.** 즉 브랜드 검색어부터 가져오는 것이 가장 빠릅니다.

| 순 | 검색어 | 난이도 | 경쟁 상대 | 담당 페이지 |
|---|---|---|---|---|
| 1 | B-CITY 춘천 | 쉬움 | 거의 없음 — 브랜드명 | 메인 |
| 2 | 춘천기업혁신파크 | 보통 | 언론사 · 춘천시 도메인 | 메인 |
| 3 | 춘천 기업도시 | 보통 | 뉴스 · 부동산 글 | 사업개요 |
| 4 | 바이오테크 이노밸리 | 쉬움 | 시행사명 — 경쟁 없음 | 사업주체 · 회사소개 |
| 5 | 기업도시개발특별법 토지확보 | 쉬움 | 법령 해설 글 — 문의로 이어짐 | 투자·입주 |
| 6 | 춘천 AI 데이터센터 | 보통 | 뉴스 · 타 지역 데이터센터 | 구역 소개 · 컨셉 |
| 7 | 하이퍼스케일 데이터센터 | 보통 | 업계 매체 — 찾는 사람이 정확함 | 컨셉 |
| 8 | GTX-B 춘천 | 보통 | 교통 뉴스 · 부동산 글 | 입지 |
| 9 | 제2경춘고속화도로 | 보통 | 위와 같음 | 입지 |
| 10 | 춘천 바이오 클러스터 | 보통 | 춘천바이오산업진흥원 등 | 구역 소개 |
| 11 | 춘천기업혁신파크 추진일정 | 쉬움 | 재방문 검색 — 경쟁 적음 | 추진일정 |
| — | `기업혁신파크`(단독) | **어려움** | 국토교통부 + 타 지역 선도사업 | 목표에서 제외 |

⚠ **등록은 순위를 올려 주지 않습니다.** 콘솔 등록이 하는 일은 ① 검색엔진이 사이트 존재를
알게 하고 ② 사이트맵을 제출하고 ③ 색인 상태와 유입 검색어를 보여 주는 것까지입니다.
순위는 그다음 경쟁입니다.

### 10.7 아직 근거 문단이 없는 검색어 — 콘텐츠가 필요합니다

아래는 검색 수요가 있는데 **사이트 본문에 설명이 없거나 표에만 있는** 것들입니다.
(괄호는 현재 본문 등장 횟수)

| 검색어 | 현재 | 필요한 것 |
|---|---|---|
| 기업 이전 지원금 | **0회** | 문단을 새로 써야 합니다 |
| 기회발전특구 혜택 | 1회 | 표는 있으나 설명 문단이 없습니다 |
| 첨단바이오 특화단지 | 1회 | 국가첨단전략산업과의 연결 설명 |
| 규제자유특구 | 2회 | 지정 현황·혜택 설명 |
| 춘천 기업 이전 세제혜택 | 3회 | 표에만 있고 설명이 없습니다 |
| 춘천기업혁신파크 입주 문의 | 13회 | 근거 있음 — 그대로 |
| 춘천 데이터센터 부지 | 10회 | 근거 있음 — 그대로 |

⚠ **검색어를 넣기 위해 없는 사실을 쓰지 마세요.** 이 사이트의 수치·일정·지분율은 전부
기획 정본과 IM 에 근거가 있고, 근거 없는 값은 의도적으로 비워 두었습니다(§9 참고).
문단이 필요하면 담당자에게 원문을 받아 쓰는 것이 맞습니다.

### 10.8 ⚠ 이 저장소와 여러분 사이트는 연결돼 있지 않습니다

확인해 둔 사실입니다(2026-09-08 실측) — 이 저장소에 커밋해도 여러분 서버에 영향이 없습니다.

| | 이 저장소 | 여러분 |
|---|---|---|
| 주소 | `mang9.github.io/bcity-homepage/` | `biotech-iv.com` |
| 서버 | GitHub Pages | 가비아 nginx |
| 연결 | 포크 0 · Pages 커스텀 도메인 없음 · `CNAME` 파일 없음 | — |

⚠ 반대로, **이 저장소에 `CNAME` 파일을 만들거나 GitHub Pages 에 커스텀 도메인을 넣으면
두 사이트가 같은 주소를 다투게 됩니다.** 도메인 이전을 실제로 진행하기 전에는 하지 마세요.

### 10.9 `biotech-iv.com` 서버에서 발견한 것 — 색인에 영향을 줍니다

2026-09-08 에 구글 서치콘솔을 등록하면서 실측한 내용입니다.
사이트맵 자체와 응답 속도에는 **문제가 없습니다** — 아래는 그 밖에서 나온 것입니다.

**✅ 정상인 것** (건드리실 필요 없습니다)

| 항목 | 실측 |
|---|---|
| `sitemap.xml` | 200 · `application/xml` · 906B · 리다이렉트 0 · `<url>` 10개 |
| XML 형식 | BOM 없음 · 선언 앞 공백 없음 · 네임스페이스 정상 · 파싱 성공 |
| 응답 속도 | 첫 바이트 **45ms** |
| `robots.txt` | 200 · `Sitemap:` 줄 포함 · Googlebot UA 로도 동일 |
| 소유확인 | DNS TXT 방식으로 완료(리졸버 8곳 전파 확인) |

**⚠ 1. 404 가 없습니다 — 없는 경로가 외부 도메인으로 302 됩니다**

```
GET https://biotech-iv.com/없는-경로
  → 302  Location: http://errdoc.gabia.net/404.html   (다른 도메인 · HTML · http)
```

- 구글은 이걸 "없는 페이지"가 아니라 **"외부 도메인으로의 임시 이동"** 으로 읽습니다.
  오타 URL · 삭제된 페이지가 전부 **소프트 404** 로 리포트에 쌓입니다.
- **사이트맵을 가져올 때 속성 도메인을 벗어나는 리다이렉트는 거부됩니다.** 즉 사이트맵
  주소를 한 글자만 틀리게 제출해도 "파일 없음"이 아니라 **「가져올 수 없음」** 으로 나옵니다.
  진단을 어렵게 만드는 원인이라 고치는 편이 좋습니다.
- 조치: 없는 경로는 **자기 도메인에서 `404` 상태코드**로 응답하게 하세요.

**⚠ 2. `http://` 가 `https://` 로 넘어가지 않습니다**

`http://biotech-iv.com/sitemap.xml` 이 리다이렉트 없이 **200** 을 줍니다.
아래 3번과 겹쳐 **같은 페이지가 최대 4가지 주소**로 존재합니다.

**⚠ 3. `www` 와 apex 가 같은 내용을 둘 다 200 으로 줍니다 — canonical 도 301 도 없습니다**

```
https://biotech-iv.com/       200   (본문 해시 동일)
https://www.biotech-iv.com/   200   (본문 해시 동일)
canonical: 두 쪽 모두 없음
```

- 구글이 대표 주소를 임의로 고릅니다. 도메인 속성으로 등록했으므로 두 주소가 한 속성에
  같이 보여 「중복 페이지」로 확인됩니다.
- 조치: `http → https`, `www → apex`(또는 반대) **두 방향 301** 을 걸거나
  최소한 `<link rel="canonical">` 을 넣으세요.

**⚠ 4. 「가져올 수 없음」이 계속되면 — 해외 IP 차단을 먼저 확인하세요**

한국 호스팅에서 흔한 원인입니다. **구글 크롤러는 해외에 있어서**, 해외 IP 차단이나
방화벽이 걸려 있으면 국내에서는 사이트가 멀쩡한데 구글만 아무것도 못 가져갑니다.

⚠ **이 항목은 저희가 확인하지 못했습니다.** 위 실측이 전부 **한국 IP(AS4766 KT)** 에서
나갔기 때문에, 해외에서의 응답은 시험 자체가 불가능했습니다.

구별법 — 서치콘솔 **URL 검사 → 실시간 테스트**(구글 서버에서 직접 가져옵니다).
국내에서는 200 인데 여기서 실패하면 네트워크 차단입니다. 그 경우 가비아 호스팅 설정에서
해외 IP 차단을 해제하거나 Googlebot 을 허용해야 합니다 — 서치콘솔에서는 고칠 수 없습니다.

**참고 · 사이트맵 제출 형식**

도메인 속성은 여러 호스트(`www`/apex · http/https)를 함께 덮으므로 사이트맵 입력란이
**전체 URL** 을 받습니다. `sitemap.xml` 처럼 경로만 넣으면 거부됩니다.

```
https://biotech-iv.com/sitemap.xml
```

⚠ 상태가 「가져올 수 없음」일 때 **재제출을 반복하지 마세요** — 상태가 바뀌지 않고
처리만 뒤로 밀립니다. 방금 제출한 직후에는 이 상태가 정상이며 보통 하루 안에 바뀝니다.
(2026-09-08 실측 — URL 검사 실시간 테스트가 성공했으므로 해외 IP 차단은 **아니었습니다.**
위 4번 항목은 앞으로 같은 증상이 나올 때의 감별용으로 남겨 둡니다.)

### 10.10 「페이지 색인이 생성되지 않는 이유」 4건 — 무엇이 정상이고 무엇이 문제인가

서치콘솔이 2026-09-08 에 보고한 4가지를 전수 추적했습니다.
**3건은 정상이고 고칠 것이 없습니다.** 실제 문제는 중복 하나입니다.

| 사유 | 페이지 | 정체 | 조치 |
|---|---|---|---|
| 리디렉션이 포함된 페이지 | **9** | 슬래시 없는 주소 9개가 **301** 로 슬래시 붙은 주소로 이동 | **없음 — 정상** |
| robots.txt에 의해 차단됨 | **1** | `/admin/` — robots.txt 의 `Disallow: /admin/` | **없음 — 의도된 것** |
| 사용자가 선택한 표준이 없는 중복 페이지 | **5** | 아래 ① ② | **고쳐야 함** |
| 크롤링됨 - 색인 생성되지 않음 | **2** | 위 중복 중 구글이 버린 쪽 | ①②를 고치면 해소 |

⚠ **이 리포트는 "문제 목록"이 아니라 "색인되지 않은 이유 목록"입니다.** 정상적인 것도
함께 나옵니다. 위 9건과 1건을 고치려고 손대면 오히려 깨집니다.

리디렉션 9건의 내역(전부 `301` → 슬래시 붙은 주소, 목표 페이지는 정상 색인됩니다):
`/notices` `/press` `/contract` `/eng` `/eng/notices` `/eng/press` `/eng/contract`
`/mobile` `/eng/mobile` — `/` 는 변형이 없어 10이 아니라 9입니다.

#### ① ⚠⚠ 영문 페이지 2개가 국문판과 **바이트 단위로 동일**합니다

```
/notices/      해시 78136bd942 · 4,133B · <html lang="ko"> · <title>공지사항
/eng/notices/  해시 78136bd942 · 4,133B · <html lang="ko"> · <title>공지사항   ← 동일
/press/        해시 f34061c662 · 7,548B
/eng/press/    해시 f34061c662 · 7,548B                                        ← 동일
```

`lang="ko"` 까지 그대로여서 **영문 페이지가 아니라 국문 페이지의 사본**입니다.
영문 3쪽의 `<title>` 이 한글(`공지사항`·`언론보도`·`관심고객등록하기`)인 것도 같은 원인입니다.
사이트맵 10쪽 중 **고유한 내용은 8쪽**뿐입니다.

- 번역이 준비돼 있으면 → 영문 내용으로 교체
- 아직이면 → **사이트맵에서 두 줄을 빼고 `noindex`** 를 넣으세요.
  지금 상태로는 구글이 중복으로 판단해 어느 쪽도 제대로 색인하지 않습니다.

#### ② 홈이 4개 주소로 존재합니다 (§10.9 의 2·3번이 합쳐진 결과)

```
https://biotech-iv.com/        │
https://www.biotech-iv.com/    │  네 개 모두 해시 20a15616fb — 완전 동일
http://biotech-iv.com/         │  canonical 없음
http://www.biotech-iv.com/     │
```

조치 두 가지 — **서버 설정 한 번으로 중복의 대부분이 사라집니다.**
1. `http → https`, `www → apex`(또는 반대) **두 방향 301**
2. 전 페이지에 `<link rel="canonical">`

#### 우선순위

**②를 먼저 하세요.** 서버 설정만으로 되고 페이지 수정이 필요 없습니다.
①은 영문 번역 여부에 대한 판단이 필요하므로 그다음입니다.

#### ③ 함께 정리하면 좋은 것 — `/mobile/`

`/mobile/`·`/eng/mobile/` 이 사이트맵에 별도 항목으로 들어 있는데, PC 쪽에
`viewport width=device-width` 가 있고 아이폰 UA 로 요청해도 리다이렉트 없이 200 입니다
(**반응형인데 별도 모바일 URL 이 함께 남아 있는 구조**). 두 쪽을 잇는 `canonical` 도
`rel="alternate"` 도 없습니다.
→ 사이트맵에서 두 줄을 빼고, 그 페이지의 `canonical` 을 `/`·`/eng/` 로 지정하세요.

#### ④ `hreflang` 이 없습니다

`/` ↔ `/eng/` 가 서로를 가리키지 않아 구글이 두 언어판을 별개 사이트로 봅니다.
상호 `hreflang="ko"` / `hreflang="en"` + `x-default` 를 넣으면 검색자 언어에 맞는 쪽을
보여줍니다. (①을 해결한 뒤에 의미가 생깁니다 — 지금은 영문 페이지가 국문 사본입니다.)

#### ⑤ `description` 이 6쪽에 없습니다

`/contract/` `/notices/` `/press/` `/eng/contract/` `/eng/notices/` `/eng/press/`.
순위 요소는 아니지만 **검색결과에 보이는 문구**를 정합니다. 없으면 구글이 본문에서
임의로 잘라 씁니다.

### 10.11 ⚠⚠ 새 사이트로 교체할 때 — 주소 체계가 완전히 다릅니다

교체 시점이 SEO 에서 **가장 위험한 순간**입니다. 옛 주소들이 이미 색인되기 시작했고,
새 사이트는 주소 구조가 전혀 다릅니다(옛: 디렉터리형 / 새: 평면 `.html`).
**301 을 걸지 않으면 색인된 주소가 전부 죽고, 클릭한 사용자는 오류 페이지를 봅니다.**

⚠ 이 서버는 없는 경로를 404 가 아니라 **외부 도메인으로 302** 합니다(§10.9-1).
  그래서 리다이렉트를 빠뜨리면 "없는 페이지"가 아니라 **다른 도메인으로 이동**이 되어,
  구글이 옛 주소의 신호를 새 주소로 옮기지 못합니다. 이 항목이 특히 중요한 이유입니다.

#### 301 대응표

| 옛 주소 (색인됨) | 새 주소 | 비고 |
|---|---|---|
| `/` | `/` | 그대로. 리다이렉트 불필요 |
| `/mobile/` | `/` **301** | 새 사이트는 반응형 단일 URL |
| `/notices/` | `/notice.html` **301** | 공지사항 |
| `/press/` | `/press.html` **301** | 언론보도 |
| `/contract/` | `/contact.html` **301** | ⚠ 아래 참고 — 페이지를 되살려야 합니다 |
| `/eng/notices/` | `/notice.html` **301** | 지금 국문 사본이라 손실 없음(§10.10-①) |
| `/eng/press/` | `/press.html` **301** | 동일 |
| `/eng/` | ⚠ **결정 필요** | 새 사이트에 영문판이 없습니다 |
| `/eng/mobile/` | ⚠ 위와 같은 판단 | |
| `/eng/contract/` | ⚠ 위와 같은 판단 | |

새 사이트의 공개 주소는 **17개**입니다 —
`index.html` `overview.html` `location.html` `effect.html` `schedule.html` `company.html`
`brand.html` `concept.html` `districts.html` `living.html` `invest.html` `about.html`
`notice.html` `press.html` `video.html` `gallery.html` `publication.html`

#### ⚠ `/contract/` 는 지금 대응 페이지가 없습니다 — 되살리는 것을 권합니다

옛 `/contract/`(관심고객등록)는 **상업적으로 가장 값어치 있는 URL** 입니다. 그런데 새
사이트에는 대응하는 페이지가 없습니다 — 문의는 **각 페이지에서 열리는 모달**입니다
(`nav.json` 의 `contact` → `href: "#contact"`, `index.html` 의 `siteMap` → `['문의하기','#contact']`).

저장소에 **숨겨 둔 페이지가 있습니다** — `src/sub/pages/_contact.html`.
2026-08-26 지시로 이름 앞에 `_` 를 붙여 빌드에서 뺀 상태입니다. 되살리는 방법:

1. 이름에서 `_` 를 뗍니다 → `contact.html` 이 다시 생성됩니다
2. `src/sub/nav.json` 의 `"href": "#contact"` → `"contact.html"`
3. `index.html` 의 `siteMap` 에서 `['문의하기','#contact']` → `['문의하기','contact.html']`
   (**2·3 을 함께** — §2 의 IA 이중 소스입니다. 한쪽만 고치면 메인과 서브가 어긋납니다)
4. 지도 CSS 를 `page-about.css` 의 `.ab-map*` 과 합칩니다(같은 모양이 두 벌이 됩니다)

⚠ **대가가 있습니다.** 지금은 어느 페이지에서든 문의 폼이 **모달로 즉시** 열립니다.
  별도 페이지로 되돌리면 페이지 이동이 생깁니다. 다만 `_contact.html` 에는 폼이 없고
  시행자 정보·찾아오시는 길만 있어서, **모달을 그대로 두고 `contact.html` 을 추가로
  살리는 것**도 가능합니다(그 페이지의 버튼이 모달을 엽니다).
  → 301 목적지로 쓸 실제 페이지가 생기므로 이쪽을 권합니다.

되살리지 않으실 경우 `/contract/` → `/` **301** 로 보내세요. 프래그먼트(`/#contact`)로
보내도 사용자에게는 모달이 열리지만, **구글은 프래그먼트를 무시하고 `/` 로 취급**합니다.

#### ⚠ 영문 5쪽 — 판단이 필요합니다

새 사이트는 **국문 전용**입니다(영문 파일 0개 · `hreflang` 0개). 옛 `/eng/` 5쪽 중
실제로 영문 내용인 것은 `/eng/`·`/eng/contract/` **둘뿐**이고 나머지 3쪽은 국문 사본입니다.

| 선택 | 결과 |
|---|---|
| 옛 `/eng/` 하위를 **그대로 남긴다** | 영문 유입을 지킵니다. 단 옛 디자인이 남아 국문/영문 화면이 달라집니다 |
| `/` 로 **301** 한다 | 화면은 일관되지만 영문 검색 유입이 사라집니다 |

영문 사이트 범위는 아직 미결입니다(§9 「영문 사이트 — 결정은 그때」). **그 결정이 나기
전까지는 옛 `/eng/` 를 남겨 두는 편이 잃는 것이 적습니다.**

#### 저장소 쪽에서 함께 해야 하는 것 — §10.1·§10.2 를 그대로

교체는 서버 파일만 바꾸는 일이 아닙니다. **이 네 가지를 빠뜨리면 새 사이트가 검색에서
사라지거나 canonical 이 깨집니다.**

1. `tools/build/pages.mjs` 의 `SITE_URL` → `https://biotech-iv.com`
2. `index.html` 의 `canonical`·`og:url`·`og:image`·JSON-LD 의 `url`·`logo`
3. `robots.txt` 의 `Sitemap:` 줄 (이제 호스트 루트에 놓이므로 **드디어 유효해집니다**)
4. **`noindex` 제거 두 곳** — `pages.mjs` 의 `NOINDEX = false` + `index.html` 의 메타태그 삭제

⚠ 4번을 빠뜨리면 **새 사이트 전체가 검색에서 빠집니다.** 1번을 바꾸고 4번을 안 바꾸면
빌드가 실패하면서 알려 주지만, 그 검사를 지우지 마세요.

#### 교체 순서

1. 위 저장소 4가지를 고치고 `npm run build` → `sitemap.xml` 이 **17 URL** 로 다시 생성됩니다
2. 서버에 새 파일 배포 + **301 대응표를 nginx 에 적용**(같은 배포에 함께)
3. 옛 `sitemap.xml` 을 새것으로 교체 — 옛 10 URL 이 남아 있으면 구글이 죽은 주소를 계속 찹니다
4. 서치콘솔에서 사이트맵 **재제출**(주소가 같으므로 새로 추가하지 않고 그대로 두면 다시 읽습니다)
5. URL 검사 → 주요 쪽 **색인 생성 요청**(`/`, `/overview.html`, `/invest.html`, `/notice.html` …)
6. 2~3주 동안 「페이지」 리포트에서 옛 주소가 「리디렉션이 포함된 페이지」로 옮겨가는지 확인

⚠ **도메인이 바뀌는 것이 아니므로 「주소 변경 도구」는 쓰지 않습니다.** 그건 도메인 이전용이고,
지금은 같은 도메인 안에서 경로만 바뀌는 것이라 301 만으로 충분합니다.
⚠ **DNS 의 `google-site-verification` TXT 를 지우지 마세요** — 지우면 소유확인이 풀립니다.

### 10.12 404 페이지 — `error_page` 설정이 필요합니다

2026-09-08 에 **`404.html` 을 새로 만들었습니다.** 지금 서버가 없는 경로를 외부 도메인
(`errdoc.gabia.net`)으로 302 하고 있어서(§10.9-1) 그 자리를 대신할 자기 도메인 404 입니다.

내용은 오류 안내 + 「메인으로 돌아가기」 + **전체 메뉴 미니 사이트맵**(대분류 4개와 하위
항목 17개)입니다. 미니 사이트맵은 `nav.json` 에서 만들어지므로 메뉴가 바뀌면 함께 바뀝니다.

#### ⚠ 파일만 두면 안 됩니다 — nginx 설정을 함께

```nginx
server {
    # …
    error_page 404 /404.html;
    location = /404.html { internal; }
}
```

- ⚠⚠ **404 상태코드와 함께 나가야 합니다.** `200` 으로 나가면 구글이 **소프트 404** 로
  쌓고, 오류 페이지가 색인 후보가 됩니다. 확인은 `curl -I https://도메인/없는경로` 로
  첫 줄이 `404` 인지 봅니다.
- ⚠ **외부 도메인으로 302 하는 현재 설정을 먼저 없애야** 합니다. 그게 남아 있으면
  `error_page` 까지 도달하지 않습니다.
- GitHub Pages 는 루트의 `404.html` 을 자동으로 이렇게 씁니다(설정 불필요).

#### ⚠ 이 파일의 주소는 전부 **루트 상대**입니다 — 그대로 두세요

404 는 요청된 주소 그대로의 URL 에서 그려집니다(`/a/b/c` 를 요청하면 브라우저 주소가
`/a/b/c` 입니다). 그래서 `assets/…` 같은 상대 경로는 `/a/b/assets/…` 로 풀려
**폰트 · 파비콘 · 메뉴 링크가 전부 깨집니다.**

빌더(`build404()`)가 `SITE_URL` 의 경로로 접두어를 붙이고, **상대 주소가 하나라도 남으면
빌드를 실패시킵니다.** 도메인을 옮기면 접두어가 자동으로 따라옵니다
(github.io 는 `/bcity-homepage/…`, 실도메인은 `/…`).

⚠ 그러므로 **`404.html` 을 손으로 고치지 마세요.** 소스는 `src/sub/pages/_404.html` +
`src/sub/css/page-404.css` 이고, `npm run build:pages` 가 다시 만듭니다.

#### 확인된 동작

| 항목 | 결과 |
|---|---|
| 깊은 경로(`/deep/x/y`)에서 렌더 | 로드 실패 **0** · 콘솔 오류 **0** (1440 · 390 양쪽) |
| GNB · 푸터 · 문의 모달 | 정상 동작(다른 페이지와 같은 셸) |
| `noindex` | 있음. `canonical` · Open Graph 는 **없음**(오류 페이지는 정본이 아닙니다) |
| `sitemap.xml` | **포함되지 않음**(17 URL 그대로) |
| 가로 넘침 | 0 |

---

## 11. 함께 보면 좋은 문서

| 파일 | 내용 |
|---|---|
| `DESIGN.md` | 디자인 토큰 SSOT (컬러 19종 · 타이포 스케일) |
| `src/sub/data/_SCHEMA.md` | 홍보센터 콘텐츠 JSON 필드 명세 |
| `admin/README.md` | 관리자 화면 21종 목록 · 기획서 대응 (`admin-design` 브랜치) |
| `tools/video/README.md` | 영상 인코딩 도구와 함정 |

> 저장소에 들어 있지 않은 내부 작업 문서(`CLAUDE.md` · `CONTENT.md`)가 있습니다.
> 상세한 구현 이력과 함정 기록이 필요하시면 담당자에게 요청하세요.
